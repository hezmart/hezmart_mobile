import 'package:flutter/material.dart';

class PickupAddressWidget extends StatelessWidget {
  const PickupAddressWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      decoration: BoxDecoration(),
    );
  }
}
